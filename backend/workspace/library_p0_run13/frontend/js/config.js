/**
 * Library P0 Run13 - Frontend Configuration
 */
const CONFIG = {
    // Backend API base URL - change this to match your backend server
    API_BASE_URL: 'http://localhost:8000',

    // Pagination defaults
    DEFAULT_PAGE_SIZE: 15,

    // Borrow limits
    MAX_BORROW_PER_READER: 5,
    DEFAULT_LOAN_DAYS: 30,

    // Refresh interval for dashboard (in milliseconds)
    DASHBOARD_REFRESH_INTERVAL: 60000,

    // Application name
    APP_NAME: '图书管理系统',
};
